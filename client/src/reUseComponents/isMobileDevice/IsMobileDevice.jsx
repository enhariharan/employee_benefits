import React, { Fragment } from 'react';

import { EMP_CONST } from '../../services/Constants';

const IsMobileDevice = React.memo(function NoRecords(props) {
  // console.log('props IsMobileDevice ', props);

  const isMobileDeviceTemplate = <Fragment></Fragment>;

  return isMobileDeviceTemplate;
});

export default IsMobileDevice;
