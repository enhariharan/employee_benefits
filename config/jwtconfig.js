"use strict";

module.exports = {
  // TODO: This is here only for dev purposes. In a prod environment, this file, and the secret below, should never ever be committed into a repo.
  secret: 'employee-benefits-app-secret',
  options: {
    expiresIn: '24h',
  }
}
